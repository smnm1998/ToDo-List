import TodoList from '@/components/TodoList';

export default function Home() {
    return (
        <main className="flex items-center jusify-center h-screen">
            <TodoList />
        </main>
    )
}